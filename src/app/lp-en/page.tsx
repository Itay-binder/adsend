// English landing — re-uses the /login page component. useLocale detects the
// /lp-en pathname and locks the page to English regardless of query params.
export { default } from '../(auth)/login/page'

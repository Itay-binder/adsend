// Hebrew landing — re-uses the /login page component. useLocale detects the
// /lp-he pathname and locks the page to Hebrew regardless of query params.
export { default } from '../(auth)/login/page'
